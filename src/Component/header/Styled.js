import styled from 'styled-components';

export const StyledHeader = styled.div`
  width: 100%;
  height: 50px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 2px 1px 3px rgba(0, 0, 0, 0.14);
  .home-img {
    width: 40px;
    height: 40px;
    margin-right: 10px;
    text-align: right;
    cursor: pointer;
  }
`;
